import{Navigate}from"react-router-dom";import{useAuth}from"../context/AuthContext";export default function ProtectedRoute({roles=[],children}){const{user}=useAuth();if(!user)return<Navigate to="/login" replace/>;if(roles.length>0&&!roles.includes(user.role))return(<div className="p-6 text-sm text-red-600">Access Denied ❌ (Role required: {roles.join(", ")})</div>);return children;}